# MongoDB connection string
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/azentrix-trello?retryWrites=true&w=majority

# JWT secret key for authentication
JWT_SECRET=azentrixsecretkey

# JWT token expiration time
JWT_EXPIRES_IN=1d

# Server port
PORT=5000

# CORS origin for frontend application
CORS_ORIGIN=http://localhost:5173
